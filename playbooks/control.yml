---
  - hosts: control
    become: true
    tasks:
      - name: install curl
        yum:
          name={{item}}
          state=present
          update_cache=yes
        with_tems:
          - curl
